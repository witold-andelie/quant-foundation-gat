# Placeholder — do NOT commit real credentials.
# In production, provision this via External Secrets Operator or Sealed Secrets.
# Replace base64 values with: echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: quant-alpha-secrets
  namespace: quant-alpha
type: Opaque
data:
  # ENTSOE_API_KEY: <base64-encoded token>
  # GCP_SA_KEY_JSON: <base64-encoded service-account JSON>
  ENTSOE_API_KEY: ""
  GCP_SA_KEY_JSON: ""
