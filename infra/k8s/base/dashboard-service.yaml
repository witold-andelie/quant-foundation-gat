apiVersion: v1
kind: Service
metadata:
  name: quant-alpha-dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: quant-alpha-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
