# Slim dependency set for Streamlit Community Cloud deployment.
# Excludes pyspark, confluent-kafka, google-cloud-*, dbt-*, dlt — none are needed
# for the read-only dashboard or synthetic-data bootstrap.
#
# Full local dev environment:  pip install -e ".[dev,dbt,ml,batch,streaming,cloud]"
streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.2.0
numpy>=1.26.0
duckdb>=0.10.0
pyarrow>=15.0.0
pyyaml>=6.0.1
pydantic>=2.6.0
yfinance>=0.2.37
typer>=0.12.0
fastavro>=1.9.4
.
